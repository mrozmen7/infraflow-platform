/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssetResponse } from '../../models/asset-response';

export interface SearchAssets$Params {

/**
 * Free text filter for id, name, type or location
 */
  searchTerm?: string;
}

export function searchAssets(http: HttpClient, rootUrl: string, params?: SearchAssets$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<AssetResponse>>> {
  const rb = new RequestBuilder(rootUrl, searchAssets.PATH, 'get');
  if (params) {
    rb.query('searchTerm', params.searchTerm, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<AssetResponse>>;
    })
  );
}

searchAssets.PATH = '/api/v1/assets';
