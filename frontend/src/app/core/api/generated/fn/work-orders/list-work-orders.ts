/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkOrderResponse } from '../../models/work-order-response';

export interface ListWorkOrders$Params {
}

export function listWorkOrders(http: HttpClient, rootUrl: string, params?: ListWorkOrders$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<WorkOrderResponse>>> {
  const rb = new RequestBuilder(rootUrl, listWorkOrders.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<WorkOrderResponse>>;
    })
  );
}

listWorkOrders.PATH = '/api/v1/work-orders';
