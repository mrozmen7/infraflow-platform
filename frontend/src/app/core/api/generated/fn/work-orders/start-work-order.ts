/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkOrderWorkflowResponse } from '../../models/work-order-workflow-response';

export interface StartWorkOrder$Params {
  workOrderId: string;
}

export function startWorkOrder(http: HttpClient, rootUrl: string, params: StartWorkOrder$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkOrderWorkflowResponse>> {
  const rb = new RequestBuilder(rootUrl, startWorkOrder.PATH, 'post');
  if (params) {
    rb.path('workOrderId', params.workOrderId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkOrderWorkflowResponse>;
    })
  );
}

startWorkOrder.PATH = '/api/v1/work-orders/{workOrderId}/start';
