/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IncidentResponse } from '../../models/incident-response';

export interface StartIncidentResponse$Params {
  incidentId: string;
}

export function startIncidentResponse(http: HttpClient, rootUrl: string, params: StartIncidentResponse$Params, context?: HttpContext): Observable<StrictHttpResponse<IncidentResponse>> {
  const rb = new RequestBuilder(rootUrl, startIncidentResponse.PATH, 'post');
  if (params) {
    rb.path('incidentId', params.incidentId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IncidentResponse>;
    })
  );
}

startIncidentResponse.PATH = '/api/v1/incidents/{incidentId}/start-response';
