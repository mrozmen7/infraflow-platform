/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IncidentPageResponse } from '../../models/incident-page-response';

export interface SearchIncidents$Params {

/**
 * Free text filter for id, asset, location or title
 */
  searchTerm?: string;

/**
 * Severity filter. Use All, Critical, High, Medium or Low.
 */
  severity?: string;

/**
 * Zero-based page index.
 */
  page?: number;

/**
 * Page size, clamped to 1-100.
 */
  size?: number;

/**
 * Sort as property,direction. Properties: reportedAt, title, severity, status, id.
 */
  sort?: string;
}

export function searchIncidents(http: HttpClient, rootUrl: string, params?: SearchIncidents$Params, context?: HttpContext): Observable<StrictHttpResponse<IncidentPageResponse>> {
  const rb = new RequestBuilder(rootUrl, searchIncidents.PATH, 'get');
  if (params) {
    rb.query('searchTerm', params.searchTerm, {});
    rb.query('severity', params.severity, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('sort', params.sort, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IncidentPageResponse>;
    })
  );
}

searchIncidents.PATH = '/api/v1/incidents';
