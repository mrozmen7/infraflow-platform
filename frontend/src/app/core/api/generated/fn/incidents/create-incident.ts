/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateIncidentRequest } from '../../models/create-incident-request';
import { IncidentResponse } from '../../models/incident-response';

export interface CreateIncident$Params {
      body: CreateIncidentRequest
}

export function createIncident(http: HttpClient, rootUrl: string, params: CreateIncident$Params, context?: HttpContext): Observable<StrictHttpResponse<IncidentResponse>> {
  const rb = new RequestBuilder(rootUrl, createIncident.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IncidentResponse>;
    })
  );
}

createIncident.PATH = '/api/v1/incidents';
