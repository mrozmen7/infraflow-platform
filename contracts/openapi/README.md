# OpenAPI Contracts

## Current file

```text
infraflow-api-v1.openapi.json
```

This file is generated from the running Spring Boot application:

```bash
curl -s -o contracts/openapi/infraflow-api-v1.openapi.json http://localhost:8080/v3/api-docs
```

Use it as the shared API contract for frontend integration, tests, documentation and
future client generation.

