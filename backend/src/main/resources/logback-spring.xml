<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <!-- Structured JSON logs for local and production runtime. Micrometer tracing
       puts traceId/spanId into the MDC and the logstash encoder ships the full MDC. -->
  <springProfile name="!test">
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <includeCallerData>false</includeCallerData>
        <fieldNames>
          <timestamp>timestamp</timestamp>
          <message>message</message>
          <logger>logger</logger>
          <thread>thread</thread>
          <levelValue>[ignore]</levelValue>
        </fieldNames>
      </encoder>
    </appender>

    <root level="${LOG_LEVEL:-INFO}">
      <appender-ref ref="JSON_CONSOLE"/>
    </root>
  </springProfile>

  <!-- Tests keep the human-readable Boot console pattern, extended with trace
       correlation ids so log assertions stay readable. -->
  <springProfile name="test">
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <property name="CONSOLE_LOG_PATTERN"
      value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd'T'HH:mm:ss.SSSXXX}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} [%X{traceId:-none},%X{spanId:-none}] %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <root level="${LOG_LEVEL:-INFO}">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>
</configuration>
