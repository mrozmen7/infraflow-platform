spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    clean-disabled: false

infraflow:
  security:
    jwt:
      # Test-only fixed value, never used outside the test suite.
      secret: infraflow-test-only-secret-with-at-least-256-bits
